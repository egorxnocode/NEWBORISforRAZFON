# 📋 Обзор проекта - Телеграм-бот с системой курса

## 🎯 Что это?

Полнофункциональный Telegram-бот с системой регистрации и автоматизированным курсом "Разгон" на 14 дней.

---

## ✨ Основные возможности

### 🔐 Регистрация
- ✅ Проверка email по предзагруженному списку
- ✅ Валидация email формата
- ✅ Проверка публичности Telegram канала
- ✅ Приветственные видео и медиа
- ✅ Система состояний пользователей

### 🎓 Система курса
- ✅ 14 дней автоматических заданий
- ✅ Рассылка в 10:00 каждый день
- ✅ 3 автоматических напоминания (8:50, 9:20, 9:35)
- ✅ Проверка выполнения в 9:50
- ✅ Система штрафов (до 4 штрафов)
- ✅ Автоматическое исключение из чата при 3 штрафах
- ✅ Кнопки "Напиши пост" и "Сдать задание"
- ✅ Отслеживание прогресса в БД

### 🛠️ Технические особенности
- ✅ Все тексты в отдельном файле (`messages.py`)
- ✅ HTML форматирование сообщений
- ✅ Планировщик задач (APScheduler)
- ✅ Supabase для БД
- ✅ Одна таблица users (не разбито на множество таблиц)
- ✅ Гибкая конфигурация
- ✅ Подробная документация

---

## 📂 Структура проекта

```
NEW BORIS/
│
├── 📄 bot.py                      # Основной файл бота
├── 📄 course.py                   # Логика курса и рассылок
├── 📄 database.py                 # Работа с Supabase
├── 📄 config.py                   # Конфигурация
├── 📄 messages.py                 # ✏️ ВСЕ ТЕКСТЫ (легко редактировать!)
│
├── 📄 requirements.txt            # Зависимости Python
├── 📄 setup_database.sql          # SQL для основной таблицы
├── 📄 setup_course_database.sql   # SQL для таблиц курса
├── 📄 example_tasks.sql           # Примеры 14 заданий
├── 📄 test_examples.py            # Тесты БД
│
├── 📄 ENV_EXAMPLE.txt             # Пример .env файла
├── 📄 БЫСТРЫЙ_СТАРТ.txt          # Быстрая инструкция
├── 📄 ИНСТРУКЦИЯ.md              # Подробная инструкция
├── 📄 README.md                   # Документация (English)
├── 📄 ОБЗОР_ПРОЕКТА.md           # Этот файл
│
└── 📁 media/                      # Медиафайлы
    ├── welcome_video.mp4          # Видеокружок
    ├── channel_request.jpg
    ├── final_message.jpg
    ├── instruction.mp4
    │
    ├── 📁 tasks/                  # 14 картинок заданий
    │   ├── task_1.jpg
    │   ├── task_2.jpg
    │   └── ... task_14.jpg
    │
    ├── 📁 penalties/              # Картинки штрафов
    │   ├── penalty_1.jpg
    │   ├── penalty_2.jpg
    │   ├── penalty_3.jpg
    │   └── penalty_4.jpg
    │
    └── 📁 reminders/              # Картинка напоминаний
        └── reminder.jpg
```

---

## 🗄️ Структура базы данных

### Таблица `users` (основная)

```sql
-- Основные поля
id                      BIGSERIAL PRIMARY KEY
email                   VARCHAR(255) UNIQUE NOT NULL
telegram_id             BIGINT UNIQUE
first_name              VARCHAR(255)
username                VARCHAR(255)
channel_link            VARCHAR(255)

-- Состояния регистрации
state                   VARCHAR(50) DEFAULT 'new'
  -- new, waiting_email, waiting_channel, registered

-- Поля курса
penalties               INT DEFAULT 0                    -- Штрафы (0-4)
current_task            INT DEFAULT 0                    -- Задание (0-14)
course_state            VARCHAR(50) DEFAULT 'not_started'
  -- not_started, in_progress, waiting_task_X, completed, excluded
last_task_completed_at  TIMESTAMP WITH TIME ZONE

-- Служебные
created_at              TIMESTAMP WITH TIME ZONE
updated_at              TIMESTAMP WITH TIME ZONE
```

### Таблица `course_tasks` (задания)

```sql
id                      SERIAL PRIMARY KEY
task_number             INT UNIQUE NOT NULL              -- 1-14
title                   VARCHAR(255) NOT NULL
description             TEXT NOT NULL                    -- HTML
image_path              VARCHAR(255)
created_at              TIMESTAMP WITH TIME ZONE
updated_at              TIMESTAMP WITH TIME ZONE
```

### Таблица `course_state` (глобальное состояние)

```sql
id                      INT PRIMARY KEY DEFAULT 1        -- Всегда 1
is_active               BOOLEAN DEFAULT FALSE
current_day             INT DEFAULT 0                    -- 0-14
start_date              TIMESTAMP WITH TIME ZONE
updated_at              TIMESTAMP WITH TIME ZONE
```

---

## 🔄 Логика работы

### Регистрация

```
1. /start
   ↓
2. Отправка видеокружка
   ↓
3. Запрос email → проверка в БД
   ↓
4. Если email OK → запись данных (telegram_id, имя)
   ↓
5. Запрос канала → проверка публичности
   ↓
6. Если канал OK → картинка + видео
   ↓
7. state = registered
```

### Курс - Запуск

```
Админ: /razgon_start
   ↓
1. course_state.is_active = TRUE
   ↓
2. Все registered → in_progress
   ↓
3. current_task = 1
   ↓
4. Отправка задания 1 СРАЗУ
```

### Курс - Ежедневный цикл

```
ДЕНЬ N (10:00):
   Рассылка задания N
   ├── Картинка
   ├── Описание
   └── Кнопки

ДЕНЬ N+1:
   08:50 → Напоминание 1
   09:20 → Напоминание 2
   09:35 → Напоминание 3
   09:50 → Проверка
      ├── Выполнил → current_task++
      └── НЕ выполнил → penalties++
            ├── 1 штраф → предупреждение
            ├── 2 штрафа → строгое предупреждение
            ├── 3 штрафа → исключение из чата
            └── 4+ штрафа → уведомление
   
   10:00 → Рассылка задания N+1
```

### Курс - Завершение

```
ДЕНЬ 14 выполнен:
   ↓
current_task = 15
   ↓
course_state = completed
   ↓
Отправка поздравления
   ↓
Курс завершен
```

---

## 🎨 Редактирование текстов

Все тексты в файле `messages.py`:

```python
# Регистрация
MSG_ASK_EMAIL = """..."""
MSG_EMAIL_NOT_FOUND = """..."""

# Курс
MSG_NEW_TASK = """..."""
MSG_REMINDER = """..."""
MSG_PENALTY_1 = """..."""

# Кнопки
BTN_WRITE_POST = "✍️ Напиши пост"
BTN_SUBMIT_TASK = "📤 Сдать задание"
```

**Просто откройте файл и измените!**

---

## ⚙️ Конфигурация

Файл `config.py`:

```python
# Время
TASK_SEND_TIME = "10:00"
REMINDER_TIMES = ["08:50", "09:20", "09:35"]
CHECK_TIME = "09:50"
TIMEZONE = "Europe/Moscow"

# Курс
COURSE_DAYS = 14

# Админы (из .env)
ADMIN_IDS = [123456789, 987654321]

# Чат курса (из .env)
COURSE_CHAT_ID = -1001234567890

# Пути к медиа
TASK_IMAGE_DIR = "media/tasks"
PENALTY_1_IMAGE = "media/penalties/penalty_1.jpg"
# ...
```

---

## 🚀 Быстрый старт

### 1. Установка

```bash
pip install -r requirements.txt
```

### 2. База данных

Выполните в Supabase SQL Editor:
1. `setup_database.sql`
2. `setup_course_database.sql`
3. `example_tasks.sql` (или свои задания)

### 3. .env файл

```env
BOT_TOKEN=ваш_токен_от_BotFather
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш_service_role_key
ADMIN_IDS=ваш_telegram_id
COURSE_CHAT_ID=id_чата_курса
TIMEZONE=Europe/Moscow
```

### 4. Медиафайлы

Добавьте в `media/`:
- Регистрация: 4 файла
- Задания: 14 картинок
- Штрафы: 4 картинки
- Напоминания: 1 картинка

### 5. Запуск

```bash
python bot.py
```

### 6. Тестирование

1. `/start` - регистрация
2. `/razgon_start` - запуск курса (от админа)

---

## 📊 Мониторинг

### Supabase Table Editor

Откройте таблицу `users`:
- Смотрите `current_task` - прогресс
- Смотрите `penalties` - штрафы
- Смотрите `course_state` - состояние

### SQL запросы

```sql
-- Активные участники
SELECT COUNT(*) FROM users WHERE course_state = 'in_progress';

-- Исключенные
SELECT COUNT(*) FROM users WHERE course_state = 'excluded';

-- Кто не сдал текущее задание
SELECT first_name, current_task, penalties 
FROM users 
WHERE current_task = (SELECT current_day FROM course_state);
```

---

## 🔧 Настройка времени

Легко изменить в `config.py`:

```python
# Рассылка в 9:00 вместо 10:00
TASK_SEND_TIME = "09:00"
REMINDER_TIMES = ["07:50", "08:20", "08:35"]
CHECK_TIME = "08:50"

# Курс на 21 день вместо 14
COURSE_DAYS = 21

# Только 2 напоминания
REMINDER_TIMES = ["09:00", "09:30"]
```

---

## ❓ Частые вопросы

### Как добавить email для регистрации?

Supabase → Table Editor → users → Insert:
```sql
INSERT INTO users (email) VALUES ('user@example.com');
```

### Как запустить курс?

Напишите боту: `/razgon_start`  
(Только от администратора, указанного в ADMIN_IDS)

### Как узнать свой Telegram ID?

Напишите боту: [@userinfobot](https://t.me/userinfobot)

### Как узнать ID чата?

Добавьте [@userinfobot](https://t.me/userinfobot) в чат

### Как перезапустить курс?

```sql
UPDATE course_state SET is_active = FALSE, current_day = 0;
UPDATE users SET course_state = 'not_started', current_task = 0, penalties = 0;
```

Затем `/razgon_start`

### Бот не отправляет рассылки?

1. Проверьте, запущен ли бот
2. Проверьте TIMEZONE в .env
3. Проверьте логи
4. Убедитесь, что курс активен:
```sql
SELECT * FROM course_state WHERE id = 1;
```

---

## 📚 Документация

- **БЫСТРЫЙ_СТАРТ.txt** - краткая инструкция
- **ИНСТРУКЦИЯ.md** - подробная инструкция с примерами
- **README.md** - документация на английском
- **ENV_EXAMPLE.txt** - пример .env файла
- **example_tasks.sql** - примеры заданий

---

## 🛠️ Технологии

- **aiogram 3.3.0** - Telegram Bot API
- **supabase** - База данных
- **APScheduler** - Планировщик задач
- **Python 3.9+** - Язык программирования
- **pytz** - Работа с временными зонами

---

## 📄 Лицензия

Этот проект создан для внутреннего использования.

---

## 🎉 Готово!

Бот полностью готов к работе. Следуйте инструкциям в **БЫСТРЫЙ_СТАРТ.txt** или **ИНСТРУКЦИЯ.md**.

**Удачи с курсом! 🚀**


# 📤 Инструкция по загрузке на GitHub

## ✅ Подготовка завершена!

Git репозиторий инициализирован и первый коммит сделан.

---

## 🔗 Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Войдите в свой аккаунт
3. Нажмите **"New repository"** (зеленая кнопка справа)

### Настройки репозитория:

- **Repository name**: `telegram-bot-course` (или любое другое имя)
- **Description**: Telegram bot for course management with AI integration
- **Visibility**: 
  - ✅ **Private** (рекомендуется, так как есть конфиденциальная логика)
  - ❌ Public (если хотите открыть исходники)
- **НЕ ОТМЕЧАЙТЕ:**
  - ❌ Add a README file
  - ❌ Add .gitignore
  - ❌ Choose a license

4. Нажмите **"Create repository"**

---

## 🚀 Шаг 2: Загрузите код на GitHub

После создания репозитория GitHub покажет инструкции. Выполните следующие команды:

### Вариант 1: Через HTTPS (проще)

```bash
cd "/Users/upsk/Documents/NEW BORIS"

# Добавьте удаленный репозиторий
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

**Замените:**
- `YOUR_USERNAME` - ваш логин на GitHub
- `YOUR_REPO_NAME` - название репозитория

### Вариант 2: Через SSH (если настроен SSH ключ)

```bash
cd "/Users/upsk/Documents/NEW BORIS"

# Добавьте удаленный репозиторий
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO_NAME.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

---

## 🔐 Аутентификация

### Если используете HTTPS:

При первом `git push` GitHub попросит войти:

**Вариант A: Personal Access Token (рекомендуется)**

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token (classic)"**
3. Выберите срок действия и права:
   - ✅ `repo` (полный доступ к репозиториям)
4. Нажмите **"Generate token"**
5. Скопируйте токен (он больше не покажется!)
6. При `git push` введите:
   - Username: ваш логин GitHub
   - Password: **вставьте токен** (не пароль!)

**Вариант B: GitHub CLI**

```bash
# Установите GitHub CLI
brew install gh  # macOS
# или скачайте с https://cli.github.com/

# Войдите
gh auth login

# Затем push будет работать автоматически
git push -u origin main
```

---

## ✅ Проверка

После успешного push:

1. Откройте https://github.com/YOUR_USERNAME/YOUR_REPO_NAME
2. Вы должны увидеть все файлы проекта
3. README_GITHUB.md будет отображаться на главной странице

---

## 📝 Пример команд (замените YOUR_USERNAME и YOUR_REPO_NAME)

```bash
cd "/Users/upsk/Documents/NEW BORIS"
git remote add origin https://github.com/YOUR_USERNAME/telegram-bot-course.git
git branch -M main
git push -u origin main
```

---

## 🎯 Следующий шаг: Развертывание на сервере

После загрузки на GitHub:

1. **Дайте мне данные сервера:**
   - IP адрес
   - Логин (обычно `root` или `ubuntu`)
   - Пароль или SSH ключ

2. **Я разверну бот на сервере:**
   - Клонирую репозиторий
   - Установлю зависимости
   - Настрою .env
   - Настрою systemd для автозапуска
   - Загружу медиафайлы

---

## 🔍 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть удаленные репозитории
git remote -v

# Добавить изменения
git add .
git commit -m "Описание изменений"
git push

# Обновить локальную версию
git pull
```

---

**Готово! После push на GitHub сообщите мне - перейдем к развертыванию на сервере! 🚀**


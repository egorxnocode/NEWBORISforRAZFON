# 🕐 Проверка возраста постов

## 📋 Описание

Бот может проверять, насколько свежий пост сдаёт пользователь. По умолчанию эта функция **ОТКЛЮЧЕНА**, так как требует дополнительных прав для бота.

---

## ⚙️ Настройка

### 1. Простой режим (по умолчанию)

По умолчанию проверка возраста постов **отключена**:

```env
CHECK_POST_AGE=false
```

В этом режиме бот:
- ✅ Проверяет формат ссылки
- ✅ Проверяет, что пост из правильного канала
- ❌ **НЕ проверяет** возраст поста

**Плюсы:**
- Не требует дополнительных прав
- Работает со всеми публичными каналами
- Проще в настройке

**Минусы:**
- Пользователи могут сдавать старые посты

---

### 2. Полная проверка (с проверкой возраста)

Чтобы включить проверку возраста постов:

```env
CHECK_POST_AGE=true
MAX_POST_AGE_HOURS=23
```

**ВАЖНО:** Для работы этого режима бот должен быть **администратором** в каждом канале пользователя!

---

## 🔧 Как добавить бота администратором

### Вариант A: Инструкция для пользователей

Попросите каждого пользователя:

1. Зайти в настройки своего канала
2. Перейти в "Администраторы"
3. Добавить бота: `@ваш_бот`
4. Дать права:
   - ✅ Публикация сообщений (опционально)
   - ✅ Редактирование сообщений (опционально)
   - ✅ Удаление сообщений (нужно минимум это)

### Вариант B: Автоматизация

К сожалению, бот не может автоматически стать администратором канала - это должен сделать владелец канала вручную.

---

## 🤔 Какой режим выбрать?

### Выбирайте **простой режим** (CHECK_POST_AGE=false), если:
- У вас много пользователей
- Вы хотите упростить регистрацию
- Не критично, если кто-то сдаст старый пост

### Выбирайте **полную проверку** (CHECK_POST_AGE=true), если:
- У вас небольшая группа пользователей
- Важно контролировать свежесть постов
- Готовы помогать пользователям добавлять бота в каналы

---

## 🐛 Устранение проблем

### Ошибка: "message to forward not found"

**Причина:** Бот не имеет доступа к каналу пользователя.

**Решение:**
1. Проверьте, что `CHECK_POST_AGE=false` в `.env`
2. Перезапустите бота: `docker restart telegram-bot`

### Ошибка: "Бот должен быть добавлен как администратор"

**Причина:** `CHECK_POST_AGE=true`, но бот не администратор канала.

**Решение:**
1. Либо добавьте бота администратором в канал
2. Либо отключите проверку: `CHECK_POST_AGE=false`

---

## 📝 Рекомендации

**Для большинства случаев рекомендуется использовать `CHECK_POST_AGE=false`**

Это упрощает работу и не требует дополнительных действий от пользователей.

---

## 🔄 Изменение настройки

Если вы изменили настройку в `.env`:

```bash
# Перезапустите бота
docker restart telegram-bot

# Или пересоберите если что-то не работает
cd /opt/NEWBORISforRAZFON
bash update-bot.sh
```

---

## ✅ Проверка

Чтобы узнать, какой режим активен:

```bash
# Посмотрите логи при сдаче задания
docker logs --tail=20 telegram-bot
```

**Если CHECK_POST_AGE=false**, вы увидите:
```
INFO - Проверка возраста поста olubimtseva/1648 отключена (CHECK_POST_AGE=false)
```

**Если CHECK_POST_AGE=true**, вы увидите:
```
INFO - Попытка получить дату поста olubimtseva/1648
```




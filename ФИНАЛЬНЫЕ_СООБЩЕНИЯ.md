# Финальные сообщения 15 дня

## Описание

После завершения 14 задания курс **не заканчивается**. На 15-й день бот отправляет 3 финальных сообщения всем пользователям в определенное время.

## Расписание отправки

| Время | Сообщение | Описание |
|-------|-----------|----------|
| **10:00** | Финальное сообщение 1 | Поздравление с завершением 14 заданий |
| **15:00** | Финальное сообщение 2 | Важная информация |
| **15:55** | Финальное сообщение 3 | Финальное прощание |

## Важные особенности

### 1. Отправка всем пользователям

Финальные сообщения отправляются **всем** пользователям, которые:
- Завершили 14 задание (current_task >= 15)
- Независимо от количества штрафов
- Независимо от того, были ли они исключены из чата

**Важно:** 
- `current_task = 14` - пользователь **НА** 14 задании (еще выполняет его)
- `current_task = 15` - пользователь **ЗАВЕРШИЛ** 14 задание

### 2. Блокировка ввода

После выдачи штрафов 14 задания (в 9:50) бот:
- ✅ **Не реагирует** на текстовые сообщения пользователей
- ✅ **Не реагирует** на голосовые сообщения
- ✅ **Не реагирует** на нажатия кнопок "Сдать задание" и "Напиши пост"
- ✅ **Только отправляет** финальные сообщения в указанное время

### 3. Автоматическое управление

- Бот автоматически определяет, что пользователь завершил 14 задание
- Финальные сообщения отправляются только один раз каждому пользователю
- Статус отправки сохраняется в БД (final_message_1_sent, final_message_2_sent, final_message_3_sent)

## Настройка в БД

### Таблица final_messages

Содержит данные о финальных сообщениях:

```sql
CREATE TABLE final_messages (
    id SERIAL PRIMARY KEY,
    message_number INTEGER UNIQUE NOT NULL,  -- 1, 2, 3
    send_time VARCHAR(5) NOT NULL,           -- "10:00", "15:00", "15:55"
    message_text TEXT NOT NULL,              -- Текст сообщения
    has_media BOOLEAN DEFAULT false,         -- Есть ли медиафайл
    media_path VARCHAR(500)                  -- Путь к медиафайлу
);
```

### Таблица users (новые поля)

Добавлены поля для отслеживания отправки:

```sql
ALTER TABLE users ADD COLUMN final_message_1_sent BOOLEAN DEFAULT false;
ALTER TABLE users ADD COLUMN final_message_2_sent BOOLEAN DEFAULT false;
ALTER TABLE users ADD COLUMN final_message_3_sent BOOLEAN DEFAULT false;
ALTER TABLE users ADD COLUMN course_finished_at TIMESTAMP WITH TIME ZONE;
```

## Установка

### 1. Выполните SQL скрипты

В Supabase SQL Editor выполните:

```bash
# 1. Создание таблицы финальных сообщений
setup_final_messages.sql

# 2. Обновление таблицы users
update_users_for_final_messages.sql
```

### 2. Настройте тексты сообщений

Отредактируйте тексты в таблице `final_messages`:

```sql
UPDATE final_messages 
SET message_text = '🎉 <b>Поздравляем!</b>

Вы завершили курс! 

Текст вашего сообщения...' 
WHERE message_number = 1;
```

### 3. Перезапустите бота

```bash
docker-compose restart telegram-bot
# или
python bot.py
```

**Примечание:** Финальные сообщения отправляются **только текстом**, без медиафайлов.

## Структура файлов

### Новые файлы

```
├── setup_final_messages.sql              # SQL: создание таблицы final_messages
├── update_users_for_final_messages.sql   # SQL: обновление таблицы users
├── final_messages_handlers.py            # Обработчик финальных сообщений
├── media/
│   └── final/
│       ├── README.md
│       ├── final_1.jpg                   # Картинка для сообщения 1
│       ├── final_2.jpg                   # Картинка для сообщения 2
│       └── final_3.jpg                   # Картинка для сообщения 3
└── ФИНАЛЬНЫЕ_СООБЩЕНИЯ.md               # Эта инструкция
```

### Обновленные файлы

- `bot.py` - добавлены планировщики и проверка should_ignore_user_input
- `database.py` - добавлены функции для работы с финальными сообщениями

## Планировщик

Добавлены 3 новых задачи в планировщик:

```python
# Финальное сообщение 1 - 10:00
scheduler.add_job(
    scheduled_final_message_1,
    CronTrigger(hour=10, minute=0, timezone=config.TIMEZONE),
    id="final_message_1"
)

# Финальное сообщение 2 - 15:00
scheduler.add_job(
    scheduled_final_message_2,
    CronTrigger(hour=15, minute=0, timezone=config.TIMEZONE),
    id="final_message_2"
)

# Финальное сообщение 3 - 15:55
scheduler.add_job(
    scheduled_final_message_3,
    CronTrigger(hour=15, minute=55, timezone=config.TIMEZONE),
    id="final_message_3"
)
```

## Логика работы

### День 14 (последний день заданий)

```
09:50 ➜ Проверка выполнения 14 задания
      ➜ Выдача штрафов
      ➜ current_day увеличивается до 15
      ➜ Бот перестает реагировать на пользователей

10:00 ➜ Обычная рассылка НЕ происходит (current_day > 14)
```

### День 15 (финальные сообщения)

```
10:00 ➜ Отправка финального сообщения 1 всем, кто завершил 14 задание
15:00 ➜ Отправка финального сообщения 2
15:55 ➜ Отправка финального сообщения 3
      ➜ Курс полностью завершен
```

## Функции API

### get_final_message(message_number: int)

Получает данные финального сообщения из БД.

```python
message_data = await get_final_message(1)
# Returns: {
#   'message_number': 1,
#   'send_time': '10:00',
#   'message_text': '...',
#   'has_media': True,
#   'media_path': 'media/final/final_1.jpg'
# }
```

### get_users_for_final_message(message_number: int)

Получает пользователей, которым нужно отправить финальное сообщение.

```python
users = await get_users_for_final_message(1)
# Returns: список пользователей с current_task >= 14 и final_message_1_sent = False
```

### send_final_message_to_all(bot: Bot, message_number: int)

Отправляет финальное сообщение всем подходящим пользователям.

```python
await send_final_message_to_all(bot, message_number=1)
```

### should_ignore_user_input(telegram_id: int)

Проверяет, нужно ли игнорировать ввод пользователя (после 14 задания).

```python
if await should_ignore_user_input(user_id):
    return  # Игнорируем
```

## Тестирование

### 1. Проверка создания таблиц

```sql
-- Проверка таблицы final_messages
SELECT * FROM final_messages;

-- Проверка полей в users
SELECT telegram_id, current_task, 
       final_message_1_sent, 
       final_message_2_sent, 
       final_message_3_sent 
FROM users 
WHERE current_task >= 14;
```

### 2. Ручная отправка финального сообщения

```python
# В консоли Python
from final_messages_handlers import send_final_message_to_all
from bot import bot
import asyncio

asyncio.run(send_final_message_to_all(bot, message_number=1))
```

### 3. Проверка игнорирования ввода

После завершения 14 задания:
1. Попробуйте отправить текстовое сообщение боту
2. Попробуйте нажать кнопки "Сдать задание" или "Напиши пост"
3. Бот **не должен** реагировать (молчит)

## Редактирование текстов

### Через SQL

```sql
-- Изменить текст первого сообщения
UPDATE final_messages 
SET message_text = 'Ваш новый текст' 
WHERE message_number = 1;

-- Изменить время отправки
UPDATE final_messages 
SET send_time = '11:00' 
WHERE message_number = 1;

-- Отключить медиафайл
UPDATE final_messages 
SET has_media = false 
WHERE message_number = 1;
```

### Важно

После изменения времени отправки (send_time) в БД, вам нужно **перезапустить бота**, чтобы планировщик обновился.

## Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи бота: `docker logs -f telegram-bot`
2. Убедитесь, что таблицы созданы правильно
3. Проверьте, что медиафайлы существуют
4. Убедитесь, что планировщик запущен

## Важные заметки

- ✅ Финальные сообщения отправляются **только один раз** каждому пользователю
- ✅ Если пользователь заблокировал бота, сообщение пропускается
- ✅ current_day должен быть >= 15 для отправки финальных сообщений
- ✅ Планировщик проверяет current_day перед отправкой
- ✅ Бот игнорирует ввод пользователей между окончанием 14 задания и получением всех 3 финальных сообщений

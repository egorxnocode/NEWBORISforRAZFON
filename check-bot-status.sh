#!/bin/bash

echo "🔍 Проверка статуса бота на сервере"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

echo "📊 1. ЛОГИ TELEGRAM-BOT (последние 50 строк):"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
docker logs --tail=50 telegram-bot
echo ""

echo "🔍 2. ПРОВЕРКА DOCKER СЕТЕЙ:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "Сети telegram-bot:"
docker inspect telegram-bot --format='{{range $k,$v := .NetworkSettings.Networks}}{{$k}}{{end}}' 2>/dev/null || echo "Контейнер не найден или остановлен"
echo ""

echo "Сети n8n-n8n-1:"
docker inspect n8n-n8n-1 --format='{{range $k,$v := .NetworkSettings.Networks}}{{$k}}{{end}}' 2>/dev/null || echo "Контейнер не найден"
echo ""

echo "Сети supabase-kong:"
docker inspect supabase-kong --format='{{range $k,$v := .NetworkSettings.Networks}}{{$k}}{{end}}' 2>/dev/null || echo "Контейнер не найден"
echo ""

echo "🔗 3. ВСЕ DOCKER СЕТИ:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
docker network ls
echo ""

echo "📝 4. СОДЕРЖИМОЕ .env:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
cat /opt/NEWBORISforRAZFON/.env | grep -v "KEY\|TOKEN\|PASSWORD" || echo "Файл .env не найден"
echo ""

echo "🐳 5. СТАТУС КОНТЕЙНЕРА:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
docker inspect telegram-bot --format='{{.State.Status}}' 2>/dev/null || echo "Контейнер не найден"
docker inspect telegram-bot --format='{{.State.Error}}' 2>/dev/null
echo ""

echo "💾 6. ПРОВЕРКА МЕДИАФАЙЛОВ:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
ls -la /opt/NEWBORISforRAZFON/media/ 2>/dev/null || echo "Директория media не найдена"
echo ""

echo "✅ Проверка завершена"


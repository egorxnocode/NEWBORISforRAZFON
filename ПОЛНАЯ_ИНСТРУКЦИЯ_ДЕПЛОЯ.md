# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ

От GitHub до запущенного бота на сервере.

---

## 📋 ЧТО УЖЕ СДЕЛАНО:

✅ Git репозиторий инициализирован  
✅ Первый коммит создан (36 файлов, 7974+ строк)  
✅ .gitignore настроен (не загружает .env и медиафайлы)  
✅ Скрипты развертывания готовы  

**Файлов в проекте:** 36  
**Строк кода:** 7974+  

---

## 🎯 ШАГ 1: ЗАГРУЗКА НА GITHUB

### 1.1. Создайте репозиторий

1. Откройте https://github.com
2. Нажмите **"New repository"**
3. Настройки:
   - **Name:** `telegram-bot-course` (или свое)
   - **Visibility:** Private (рекомендуется)
   - **НЕ отмечайте:** README, .gitignore, license
4. Нажмите **"Create repository"**

### 1.2. Загрузите код

Скопируйте команды, которые GitHub покажет после создания репозитория.

**Выполните на вашем Mac:**

```bash
cd "/Users/upsk/Documents/NEW BORIS"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Отправьте код
git branch -M main
git push -u origin main
```

### 1.3. Аутентификация

GitHub попросит войти:

**Вариант A: Personal Access Token (проще)**

1. Перейдите: https://github.com/settings/tokens
2. "Generate new token (classic)"
3. Выберите срок и права: `repo`
4. Скопируйте токен
5. При push:
   - Username: ваш логин
   - Password: **вставьте токен**

**Вариант B: GitHub CLI**

```bash
brew install gh
gh auth login
git push -u origin main
```

### 1.4. Проверка

Откройте https://github.com/YOUR_USERNAME/YOUR_REPO

Вы должны увидеть все файлы!

---

## 🖥️ ШАГ 2: РАЗВЕРТЫВАНИЕ НА СЕРВЕРЕ

### 2.1. Подготовка

**Вам нужно:**
- IP адрес сервера
- Логин (обычно `root` или `ubuntu`)
- Пароль или SSH ключ
- Ubuntu 20.04+ / Debian 11+

### 2.2. Подключение к серверу

```bash
ssh user@YOUR_SERVER_IP
```

Если пароль:
```bash
sshpass -p 'YOUR_PASSWORD' ssh user@YOUR_SERVER_IP
```

### 2.3. Автоматическое развертывание

**На сервере выполните:**

```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите git
sudo apt install -y git

# Клонируйте репозиторий временно
cd /tmp
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git bot-deploy
cd bot-deploy

# Отредактируйте deploy.sh (замените YOUR_USERNAME и YOUR_REPO на свои)
nano deploy.sh

# Найдите строку:
# git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git telegram-bot
# Замените на свой репозиторий

# Сохраните: Ctrl+O, Enter, Ctrl+X

# Сделайте скрипт исполняемым
chmod +x deploy.sh

# Запустите
./deploy.sh
```

Скрипт автоматически:
- Установит Python и зависимости
- Клонирует репозиторий в `/opt/telegram-bot`
- Создаст виртуальное окружение
- Установит библиотеки
- Создаст systemd сервис
- Настроит автозапуск

### 2.4. Настройка .env

**На сервере:**

```bash
nano /opt/telegram-bot/.env
```

**Заполните:**

```env
BOT_TOKEN=ваш_токен_от_BotFather
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш_service_role_ключ
ADMIN_IDS=ваш_telegram_id
COURSE_CHAT_ID=-1001234567890
MONITORING_CHAT_ID=-1001234567891
OPENAI_API_KEY=sk-ваш_ключ
N8N_WEBHOOK_URL=https://ваш-n8n.com/webhook
TIMEZONE=Europe/Moscow
```

**Сохраните:** Ctrl+O, Enter, Ctrl+X

### 2.5. Загрузка медиафайлов

**С вашего Mac:**

```bash
# Загрузите все медиа (замените user и server-ip)
scp -r "/Users/upsk/Documents/NEW BORIS/media"/* user@server-ip:/opt/telegram-bot/media/
```

Или через FileZilla/WinSCP:
1. Подключитесь к серверу
2. Перейдите в `/opt/telegram-bot/media/`
3. Загрузите файлы

**Нужные файлы:**
```
media/
├── welcome_video.mp4
├── channel_request.jpg
├── final_message.jpg
├── instruction.mp4
├── tasks/
│   ├── task_1.jpg
│   ├── task_2.jpg
│   └── ... task_14.jpg
├── penalties/
│   └── penalty.jpg
└── reminders/
    ├── reminder_1.jpg
    ├── reminder_2.jpg
    └── reminder_3.jpg
```

### 2.6. Запуск бота

**На сервере:**

```bash
# Запустить
sudo systemctl start telegram-bot

# Проверить статус
sudo systemctl status telegram-bot

# Смотреть логи
sudo journalctl -u telegram-bot -f
```

**Вы должны увидеть:**
```
Бот запущен!
Временная зона: Europe/Moscow
Администраторы: [ваш_id]
Планировщик запущен!
```

---

## ✅ ШАГ 3: ПРОВЕРКА РАБОТЫ

### 3.1. Telegram

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Проверьте регистрацию

### 3.2. Админ команды

```
/razgon_start          - Запустить курс
/send_digest all       - Отправить задание всем
/850                   - Тест напоминания
```

### 3.3. Мониторинг

Проверьте, что отчеты приходят в чат мониторинга.

---

## 🔧 УПРАВЛЕНИЕ БОТОМ

### Команды

```bash
# Запуск
sudo systemctl start telegram-bot

# Остановка
sudo systemctl stop telegram-bot

# Перезапуск
sudo systemctl restart telegram-bot

# Статус
sudo systemctl status telegram-bot

# Логи (в реальном времени)
sudo journalctl -u telegram-bot -f

# Последние 100 строк
sudo journalctl -u telegram-bot -n 100
```

### Обновление бота

```bash
cd /opt/telegram-bot
sudo systemctl stop telegram-bot
git pull
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl start telegram-bot
```

---

## 🔥 ОТКРЫТИЕ ПОРТА 8080 (для n8n)

```bash
# Если используете ufw
sudo ufw allow 8080/tcp
sudo ufw enable
sudo ufw status

# Или для конкретного IP n8n
sudo ufw allow from N8N_IP to any port 8080
```

---

## 🐛 УСТРАНЕНИЕ ПРОБЛЕМ

### Бот не запускается

```bash
# Смотрим логи
sudo journalctl -u telegram-bot -n 50

# Проверяем .env
cat /opt/telegram-bot/.env

# Проверяем права
ls -la /opt/telegram-bot/
```

### Ошибка с медиафайлами

```bash
# Проверяем наличие
ls -la /opt/telegram-bot/media/
ls -la /opt/telegram-bot/media/tasks/

# Права
sudo chmod -R 755 /opt/telegram-bot/media/
```

### Ошибка с Supabase

- Проверьте SUPABASE_URL и SUPABASE_KEY
- Проверьте, что таблицы созданы
- Проверьте доступ к интернету на сервере

---

## 📊 МОНИТОРИНГ

Бот автоматически отправляет отчеты в чат:
- Рассылки (10:00)
- Напоминания (8:50, 9:20, 9:35)
- Штрафы (9:50)
- Таймауты n8n
- Ежедневная сводка (23:59)

---

## 🎯 ЧЕКЛИСТ ДЕПЛОЯ

### GitHub
- [ ] Репозиторий создан
- [ ] Код загружен (`git push`)
- [ ] Репозиторий виден на GitHub

### Сервер
- [ ] SSH доступ есть
- [ ] Ubuntu/Debian установлен
- [ ] Скрипт deploy.sh запущен
- [ ] .env настроен
- [ ] Медиафайлы загружены
- [ ] Бот запущен
- [ ] Логи без ошибок
- [ ] `/start` работает

### Настройки
- [ ] Supabase таблицы созданы
- [ ] Email'ы добавлены в users
- [ ] Задания заполнены (digest_day_1 ... 14)
- [ ] Чат курса создан
- [ ] Чат мониторинга создан
- [ ] n8n настроен (опционально)

---

## 💡 РЕКОМЕНДАЦИИ

1. **Резервное копирование .env:**
   ```bash
   scp user@server:/opt/telegram-bot/.env ~/backup/.env
   ```

2. **Мониторинг сервера:**
   - Установите `htop` для мониторинга ресурсов
   - Настройте алерты при падении бота

3. **Безопасность:**
   - Используйте SSH ключи вместо паролей
   - Настройте firewall (ufw)
   - Регулярно обновляйте систему

4. **Логирование:**
   ```bash
   # Автоматическая ротация логов
   sudo nano /etc/systemd/journald.conf
   # SystemMaxUse=500M
   ```

---

## 📞 ПОСЛЕ ДЕПЛОЯ

**Когда все готово, дайте мне знать:**

✅ Репозиторий на GitHub создан  
✅ Бот развернут на сервере  
✅ Бот запущен и работает  

**И я помогу с:**
- Проверкой настроек
- Устранением проблем
- Оптимизацией работы
- Настройкой дополнительных функций

---

## 📚 ДОКУМЕНТАЦИЯ

- **DEPLOY_GITHUB.md** - детали загрузки на GitHub
- **DEPLOY_SERVER.md** - детали развертывания
- **БЫСТРЫЙ_СТАРТ.txt** - быстрая настройка
- **ИНСТРУКЦИЯ.md** - полная документация
- **МОНИТОРИНГ.md** - система мониторинга

---

**Все готово для деплоя! 🚀**

Следуйте шагам и сообщите, если нужна помощь!


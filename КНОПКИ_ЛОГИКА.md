# Логика работы кнопок в боте

## 🎯 Обзор

После обновления кнопки "Напиши пост" и "Сдать задание" работают **только в определенных состояниях**, чтобы предотвратить преждевременную сдачу заданий.

---

## 🔘 Кнопка "Напиши пост"

### ✅ Когда ДОСТУПНА:

1. **Обычные пользователи:**
   - Состояние: `IN_PROGRESS` (получили задание, еще не сдали)
   - После рассылки в 10:00

2. **LIMITED пользователи (опоздавшие):**
   - **ВСЕГДА доступна** независимо от состояния
   - Могут писать посты круглосуточно

### ❌ Когда ЗАБЛОКИРОВАНА:

**Для обычных пользователей:**
- Состояние: `waiting_task_2`, `waiting_task_3`, и т.д.
- Сообщение: *"⏳ Ожидайте следующее задание. Кнопки станут активны после получения задания."*

**Для всех:**
- Состояния: `not_started`, `completed`, `excluded`, и др.
- Сообщение: *"Вы не участвуете в курсе"*

---

## 🔘 Кнопка "Сдать задание"

### ✅ Когда ДОСТУПНА:

**Только для всех пользователей (включая LIMITED):**
- Состояние: `IN_PROGRESS`
- После рассылки в 10:00

### ❌ Когда ЗАБЛОКИРОВАНА:

**Для всех пользователей (включая LIMITED):**
- Состояние: `waiting_task_X`
- Сообщение: *"⏳ Ожидайте следующее задание. Кнопки станут активны после получения задания."*

**Для всех:**
- Состояния: `not_started`, `completed`, `excluded`, и др.
- Сообщение: *"Вы не участвуете в курсе"*

---

## 📊 Таблица состояний

| Состояние | "Напиши пост" (обычные) | "Напиши пост" (LIMITED) | "Сдать задание" (все) |
|-----------|------------------------|------------------------|----------------------|
| `in_progress` | ✅ Доступна | ✅ Доступна | ✅ Доступна |
| `waiting_task_2` | ❌ Заблокирована | ✅ Доступна | ❌ Заблокирована |
| `waiting_task_3` | ❌ Заблокирована | ✅ Доступна | ❌ Заблокирована |
| `limited` | N/A | ✅ Доступна | ❌ Заблокирована |
| `completed` | ❌ Не в курсе | ❌ Не в курсе | ❌ Не в курсе |
| `excluded` | ❌ Не в курсе | ❌ Не в курсе | ❌ Не в курсе |
| `not_started` | ❌ Не в курсе | ❌ Не в курсе | ❌ Не в курсе |

---

## 🔄 Жизненный цикл обычного пользователя

```
1. Регистрация
   → registered
   
2. Старт курса
   → waiting_task_1
   ❌ Кнопки заблокированы
   
3. Рассылка задания 1 (10:00)
   → in_progress
   ✅ Обе кнопки доступны
   
4. Пользователь сдал задание
   → waiting_task_2
   ❌ Кнопки заблокированы
   
5. Рассылка задания 2 (10:00)
   → in_progress
   ✅ Обе кнопки доступны
   
... и так далее
```

---

## 🔄 Жизненный цикл LIMITED пользователя

```
1. Регистрация (опоздал на день 2+)
   → limited
   ✅ "Напиши пост" доступна ВСЕГДА
   ❌ "Сдать задание" только в in_progress
   
2. Рассылка задания (10:00)
   → limited (состояние НЕ меняется)
   ✅ "Напиши пост" доступна
   ✅ "Сдать задание" доступна (current_task обновляется)
   
3. После сдачи задания
   → limited (состояние НЕ меняется)
   ✅ "Напиши пост" доступна ВСЕГДА
   ❌ "Сдать задание" ждет следующей рассылки
```

---

## 💡 Почему такая логика?

### Проблема:
Пользователи нажимали старые кнопки в диалоге и сдавали задания **раньше времени**, до получения нового задания.

### Решение:
1. **Обычные пользователи:**
   - Кнопки активны **только** во время выполнения задания (`in_progress`)
   - После сдачи переходят в `waiting_task_X` и кнопки блокируются
   - Получают новое задание в 10:00 → кнопки снова активны

2. **LIMITED пользователи (опоздавшие):**
   - Имеют **постоянный доступ** к "Напиши пост"
   - Могут писать посты независимо от времени суток
   - Сдача задания работает так же как у обычных (только в `in_progress`)

---

## 🛠️ Технические детали

### Код проверки (bot.py):

#### Кнопка "Напиши пост":
```python
course_state = await get_user_course_state(user_id)

# LIMITED могут писать всегда
if course_state == CourseState.LIMITED:
    pass  # Разрешаем
    
# Обычные только в IN_PROGRESS
elif course_state != CourseState.IN_PROGRESS:
    if course_state.startswith("waiting_task"):
        await callback.answer("⏳ Ожидайте следующее задание...")
    return
```

#### Кнопка "Сдать задание":
```python
course_state = await get_user_course_state(user_id)

# Все пользователи (включая LIMITED) только в IN_PROGRESS
if course_state != CourseState.IN_PROGRESS:
    if course_state.startswith("waiting_task"):
        await callback.answer("⏳ Ожидайте следующее задание...")
    return
```

---

## 🔍 Диагностика

### Проверка состояния пользователя в БД:

```sql
SELECT 
    telegram_id,
    course_state,
    current_task,
    last_task_completed_at
FROM users
WHERE telegram_id = 123456789;
```

### Возможные состояния:
- `registered` - зарегистрирован, курс не начался
- `in_progress` - выполняет задание
- `waiting_task_1`, `waiting_task_2`, ... `waiting_task_14` - ждет следующего задания
- `limited` - опоздавший участник
- `completed` - завершил курс
- `excluded` - исключен

---

## 📝 История изменений

### Версия 2.0 (27.01.2026)
- ✅ Добавлена блокировка кнопок в `waiting_task_X`
- ✅ Добавлено исключение для LIMITED пользователей
- ✅ Информативные сообщения при нажатии

### Версия 1.0 (до 27.01.2026)
- Кнопки работали всегда (любое состояние)
- Пользователи могли сдавать задания раньше времени

---

## ❓ FAQ

### В: Что видит пользователь при нажатии заблокированной кнопки?
**О:** Всплывающее окно с текстом: *"⏳ Ожидайте следующее задание. Кнопки станут активны после получения задания."*

### В: Когда кнопки становятся активными?
**О:** После рассылки задания в 10:00, когда пользователь переходит в состояние `in_progress`.

### В: Могут ли LIMITED пользователи сдавать задания в любое время?
**О:** Нет! Кнопка "Сдать задание" работает только в `in_progress` для **всех** пользователей, включая LIMITED. Но "Напиши пост" доступна им всегда.

### В: Что если пользователь не сдал задание вовремя?
**О:** Он останется в состоянии `in_progress` до сдачи. После сдачи перейдет в `waiting_task_X` и будет ждать следующей рассылки.

### В: Можно ли вернуть старую логику (кнопки всегда активны)?
**О:** Да, но это не рекомендуется. Нужно удалить проверки состояния в `bot.py`.

---

**Последнее обновление:** 27.01.2026  
**Версия:** 2.0

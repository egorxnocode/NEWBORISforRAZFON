# Telegram Bot с регистрацией через Supabase

Телеграм-бот для регистрации пользователей с проверкой email и канала через базу данных Supabase.

## Возможности

- Регистрация пользователей по предварительно загруженным email
- Проверка валидности email
- Проверка публичности Telegram каналов
- Отправка приветственных видео и инструкций
- Сохранение состояний пользователей в БД

## Установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Настройка Supabase

Создайте таблицу `users` в Supabase со следующей структурой:

```sql
CREATE TABLE users (
    id BIGSERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    telegram_id BIGINT UNIQUE,
    first_name VARCHAR(255),
    username VARCHAR(255),
    channel_link VARCHAR(255),
    state VARCHAR(50) DEFAULT 'new',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- Индекс для быстрого поиска по email
CREATE INDEX idx_users_email ON users(email);

-- Индекс для быстрого поиска по telegram_id
CREATE INDEX idx_users_telegram_id ON users(telegram_id);
```

### 3. Заполните emails в БД

Вручную добавьте email пользователей, которым разрешена регистрация:

```sql
INSERT INTO users (email) VALUES ('user1@example.com');
INSERT INTO users (email) VALUES ('user2@example.com');
-- и так далее...
```

### 4. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые данные:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```
BOT_TOKEN=ваш_токен_бота_от_BotFather
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш_service_role_ключ
```

### 5. Добавьте медиафайлы

Создайте папку `media` и добавьте в нее следующие файлы:

```
media/
├── welcome_video.mp4      # Видеокружок для приветствия
├── channel_request.jpg    # Картинка при запросе канала
├── final_message.jpg      # Картинка после успешной регистрации
└── instruction.mp4        # Видео с инструкцией
```

**Важно:** Для видеокружка (`welcome_video.mp4`) используйте круглое видео (aspect ratio 1:1).

## Запуск

```bash
python bot.py
```

## Структура проекта

```
.
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация и настройки
├── database.py         # Работа с базой данных Supabase
├── messages.py         # Все текстовые сообщения (легко редактировать!)
├── requirements.txt    # Зависимости Python
├── .env               # Переменные окружения (создайте из .env.example)
├── .env.example       # Пример файла с переменными
└── media/             # Папка с медиафайлами
    ├── welcome_video.mp4
    ├── channel_request.jpg
    ├── final_message.jpg
    └── instruction.mp4
```

## Редактирование текстов

Все тексты сообщений находятся в файле `messages.py`. 
Просто откройте его и отредактируйте нужные сообщения. 
Поддерживается HTML форматирование.

Пример:

```python
MSG_ASK_EMAIL = """
Привет! 👋

Пожалуйста, введите ваш <b>email</b> для регистрации.
"""
```

## Как работает регистрация

1. Пользователь отправляет команду `/start`
2. Бот отправляет приветственное видеокружок
3. Бот запрашивает email
4. Проверяется валидность email и его наличие в БД
5. Если email правильный - запрашивается ссылка на канал
6. Проверяется, что канал публичный
7. После успешной проверки отправляется картинка и видео с инструкцией
8. Пользователь зарегистрирован!

## Состояния пользователя

- `new` - новый пользователь
- `waiting_email` - ожидание ввода email
- `waiting_channel` - ожидание ввода ссылки на канал
- `registered` - полностью зарегистрирован

## Примечания

- Все сообщения отправляются в формате HTML
- Медиафайлы хранятся локально на сервере
- Состояния пользователей сохраняются в БД
- Все данные хранятся в одной таблице `users`

## Получение токена бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env` файл

## Получение ключей Supabase

1. Откройте ваш проект на [supabase.com](https://supabase.com)
2. Перейдите в Settings → API
3. Скопируйте `URL` и `service_role key`
4. Вставьте их в `.env` файл

